apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-v2
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-v2
  template:
    metadata:
      labels:
        app: nginx-v2
    spec:
      containers:
      - name: nginx
        image: nginx:8c7ed49-1690868944
        ports:
        - containerPort: 80
