apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-v2
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-v2
  template:
    metadata:
      labels:
        app: nginx-v2
    spec:
      containers:
      - name: nginx
        image: nginx:625943d-1690870720
        ports:
        - containerPort: 80
